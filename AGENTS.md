# Agent Instructions

- When updating caching behavior or offline support, document how users can force a cache refresh (e.g., reinstall the PWA or clear site data) and whether a cache version bump is required.
